# Homework 4 (APMA 4302) — Submission Package

**Main submission PDF:** `HW4_4302.pdf`  

- The **solutions write-up** is contained in that PDF.
- Source LaTeX lives in `doc/` (see `doc/README.md`).
- Code used for Problems 1–4 is under `python/`, `scripts/`, and `c/`.
- Figures referenced by the write-up are under `doc/figures/`.
- The Q4 $Nu(t)$ histories used for the plots are under `doc/data/New/`.

